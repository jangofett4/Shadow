#pragma once

#include "Extensions.hh"
#include <vector>

namespace std {
    /*
    template<typename T, typename X, typename Z>
    std::vector<Z> select(T begin, T end, X functor)
    {
        std::vector<X> result;
        for (auto it = begin; it != end; it++)
            result.push_back(functor(*it));
        return result;
    }
    */
}